# [사용 안 함] 이 컨테이너에는 /etc/nginx 자체가 없고 nginx를 설치/설정할 권한도
# 없습니다 (root 아님). 이 서버에서는 가비아 플랫폼이 컨테이너 바깥에서 도메인 라우팅과
# (아마도) SSL 종료까지 대신 처리하고, 컨테이너 안의 앱은 그냥 $PORT(=8080)로만 떠
# 있으면 되는 구조로 보입니다.
#
# 도메인 연결 / SSL 인증서 설정은 My가비아 > 서비스 관리 > 컨테이너 호스팅 관리 콘솔에서
# 진행해야 합니다 (셸 안에서 할 수 있는 작업이 아님). 콘솔 화면을 캡처해서 보여주시면
# 정확히 어디서 설정하는지 같이 확인해드릴게요.
